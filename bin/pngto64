#!/usr/bin/python

import sys

if len(sys.argv) == 1:
    data = sys.stdin.read()
elif len(sys.argv) == 2:
    with open(sys.argv[1]) as f:
        data = f.read()

print "data:image/png;base64," + data.encode("base64").replace("\n", "")
