#!/bin/zsh

VUNDLE=~/.dotfiles/vim/vim.symlink/bundle/vundle
if [[ -e $VUNDLE ]]; then
    echo "$VUNDLE exists; not overwriting."
else
    @ git clone https://github.com/gmarik/vundle.git $VUNDLE
    @ vundle install
fi
