#!/bin/zsh

VIMPLUG=~/.vim/autoload/plug.vim
if [[ ! -e $VIMPLUG ]]; then
    URL=https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
    @ curl -fLo $VIMPLUG --create-dirs $URL
    @ bin/vim-plug install
fi
if [[ ! -e ~/.vimswp ]]; then
    @ mkdir ~/.vimswp
fi
